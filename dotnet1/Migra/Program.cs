using System;

namespace Migra
{
    class Program
    {
        static void Main(string[] args)
        {
        //    /create db by migration
            // create db: dotnet ef migrations add InitWebDB
            //check list migrations: dotnet ef migrations list 
            // del version lass: dotnet ef migrations remove
            // update: dotnet ef database update [Migration]
            // del db: update: dotnet ef databsase drop
// return all sql:dotnet ef migrations script Name1 Name2
// return all file sql:dotnet ef migrations script --output migrations.sql

        }
    }
}
